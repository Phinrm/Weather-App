# Frontend (React + Vite)

## Setup

```bash
cd frontend
npm install
npm run dev
```

By default the app expects the backend at `http://localhost:8000/api`.

You can override this using a `.env` file with:

```bash
VITE_API_BASE=http://localhost:8000/api
```
